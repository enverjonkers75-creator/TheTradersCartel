export type AppRole = "student" | "owner" | "developer";
export type MemberStatus = "pending" | "active" | "rejected" | "suspended";
export type Profile = { id: string; email: string; full_name: string; role: AppRole; status: MemberStatus; group_no: 1 | 2 | 3 | null; avatar_path: string | null; timezone: string };
export type TradingAccount = { id: string; user_id: string; name: string; broker: string | null; currency: string; starting_balance: number; current_balance: number; is_primary: boolean; verified_at: string | null; created_at: string };
export type Trade = { id: string; user_id: string; account_id: string; symbol: string; side: "buy" | "sell"; lot_size: number; opened_at: string; closed_at: string; entry_price: number; exit_price: number; stop_loss: number; take_profit: number; gross_pnl: number; commission: number; swap: number; net_pnl: number; setup: string; reason: string; emotion: string; followed_plan: boolean; reflection: string | null; risk_amount: number; reward_amount: number; risk_reward: number | null; achieved_r: number | null; created_at: string };
export type BalanceVerification = { id: string; account_id: string; user_id: string; submitted_balance: number; currency: string; fx_rate_to_zar: number | null; zar_equivalent: number | null; proof_path: string; status: "pending" | "approved" | "rejected"; review_note: string | null; created_at: string; profiles?: Pick<Profile, "full_name" | "email" | "group_no">; trading_accounts?: Pick<TradingAccount, "name"> };
export type Channel = { id: string; slug: string; name: string; kind: "group" | "announcements"; group_no: number | null };
export type MemberDirectory = { id: string; full_name: string; role: AppRole; group_no: number | null; avatar_path: string | null };
export type Conversation = { id: string; created_at: string; conversation_participants?: Array<{ user_id: string; member_directory?: MemberDirectory }> };
export type Message = { id: string; channel_id: string | null; conversation_id: string | null; sender_id: string; kind: "text" | "image" | "trade"; body: string; attachment_path: string | null; shared_trade_snapshot: Record<string, unknown> | null; edited_at: string | null; deleted_at: string | null; created_at: string; member_directory?: MemberDirectory };
export type JournalEntry = { id: string; user_id: string; symbol: string; side: "buy" | "sell"; pnl_amount: number; currency: string; traded_at: string; notes: string; screenshot_path: string | null; created_at: string; updated_at: string };
